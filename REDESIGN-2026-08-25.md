# สรุปงาน Redesign — 2026-08-25

ปรับดีไซน์ให้สวยขึ้นและเพิ่มระบบใหม่ โดยอ้างอิงจาก https://stratz.com/

> เอกสารนี้คือ **บันทึกสรุปงานรอบนี้** (snapshot) ไม่ใช่คู่มืออ้างอิงที่อัปเดตตลอด
> สำหรับคู่มือการพัฒนาที่เป็นปัจจุบัน ให้ดู [DEVELOPMENT.md](DEVELOPMENT.md)

---

## 1. วิธีอ้างอิงจาก STRATZ

แทนที่จะเดาว่า "สไตล์ STRATZ น่าจะประมาณนี้" ผมเปิด stratz.com ในเบราว์เซอร์แล้ว
**อ่านค่า computed style จริงของเว็บเขาออกมา** (สุ่มตัวอย่างจากทั้งหน้าแรกและหน้า `/heroes`)

ผลคือพบว่าธีมเดิมของโปรเจกต์เดาผิดอยู่หลายจุด:

| สิ่งที่พบจริงบน stratz.com | ธีมเดิมของเรา | สรุป |
|---|---|---|
| สีแบรนด์คือ **เขียวน้ำทะเล (teal)** `#10A4C1`, gradient `#0BAFD0 → #078197` | ใช้ฟ้า `#4C9BE8` | ต้องเปลี่ยน |
| ทอง `#D6AF4C` (ใช้เยอะมาก 103 จุด) | `#CBB02A` | ปรับให้ตรง |
| การ์ดใช้ **inset hairline + เงาอ่อน ๆ** ไม่ใช่เส้นขอบ | ใช้ `border` 1px | ต้องเปลี่ยน |
| พื้นผิวที่ซ้อนในการ์ดเป็น **ขาวโปร่งใส** `rgba(255,255,255,0.04)` / `0.08` | ใช้สีทึบชั้นที่สอง | ต้องเปลี่ยน |
| พื้นดำสนิท `#000000`, การ์ด `#141414` | ตรงอยู่แล้ว | ✅ ถูกแล้ว |
| เขียวชนะ `#2ACB4F`, radius 4px | ตรงอยู่แล้ว | ✅ ถูกแล้ว |

นอกจากนี้ยังพบว่า **GraphQL introspection ของ STRATZ เปิดอยู่** ซึ่งทำให้เจอข้อมูลที่มี
อยู่แล้วแต่เรายังไม่ได้ดึงมาใช้อีก 4 ชุด (กลายเป็นฟีเจอร์ใหม่ในข้อ 3)

---

## 2. Design System

### 2.1 Design tokens

ไฟล์: [`tailwind.config.ts`](tailwind.config.ts), [`src/app/globals.css`](src/app/globals.css)

| บทบาท | ค่า | Token |
|---|---|---|
| พื้นหน้า | `#000000` | `bg-bg-primary` |
| การ์ด | `#141414` | `bg-bg-card` |
| พื้นผิวซ้อนในการ์ด | `rgba(255,255,255,0.04)` / `0.08` | `bg-bg-overlay` / `-strong` |
| เส้นแบ่ง (hairline) | `rgba(255,255,255,0.08)` | วาดเป็น **inset ring** ไม่ใช่ border |
| แบรนด์ / ปุ่มกด / ลิงก์ | teal `#10A4C1` | `accent-teal`, `bg-brand-gradient` |
| ปริมาณ / จำนวน | ทอง `#D6AF4C` | `accent-gold` |
| ชนะ / แพ้ | `#2ACB4F` / `#EC041F` | `win` / `loss` |
| Radiant / Dire | `#4FA855` / `#C23C2A` | แยกจาก win/loss โดยตั้งใจ |
| เหรียญแรงก์ Herald→Immortal | 8 สี | `rank.*` + `rankTierColor()` |
| Radius | 4px ทุกที่ | `--radius` |

**กติกา 2 ข้อที่พังง่ายถ้าไม่รู้:**

1. **การ์ดไม่มี `border`** — ความลึกมาจาก inset hairline + เงาอ่อน (`shadow-card`)
   เพราะถ้าใส่เส้นขอบจริงทุกแผง หน้าที่ข้อมูลแน่นขนาดนี้จะกลายเป็นตารางเส้นเต็มไปหมด
   ใช้คลาส `.card` หรือ `<Card>` และใช้ `.ring-hairline` กับอย่างอื่นที่ต้องการขอบ
2. **`accent-blue` (`#4C9BE8`) ตอนนี้เหลือไว้เป็นสีของเส้นกราฟเท่านั้น**
   (PoolScatter, RadarAxes, BenchmarkTimeline, ImpVsMmrChart)
   ส่วนลิงก์ / สถานะ active / ปุ่มกดทั้งหมด ใช้ `accent-teal` — **อย่าเอาฟ้ากลับมาใช้กับ UI chrome**

### 2.2 Primitives ใหม่ — `src/components/ui/`

| Component | ใช้ทำอะไร |
|---|---|
| `Card`, `CardHeader`, `CardBody`, `CardLinkAction` | ทุกแผง; `padded={false}` สำหรับตาราง/กราฟที่ต้องชิดขอบ |
| `StatTile`, `DeltaBadge` | ตัวเลขเดี่ยวทุกจุด พร้อม delta / sparkline / meter |
| `Badge`, `ResultBadge`, `RankBadge` | ป้าย pill ต่าง ๆ |
| `BenchmarkMeter`, `SplitBar`, `ProgressBar` | แถบบาร์ — `BenchmarkMeter` วาดขีด p25/p50/p75 ไว้หลังค่า |
| `Sparkline` | เส้นเทรนด์เล็ก ๆ (SVG เขียนเอง ใช้ใน server component ได้) |
| `HeroAvatar`, `HeroCell` | รูปฮีโร่ และเซลล์ฮีโร่มาตรฐานในตาราง |
| `Tooltip` | tooltip แบบ CSS ล้วน ไม่มี state — ปลอดภัยใน server component และในกริดใหญ่ ๆ |
| `SegmentedControl` | ตัวสลับตัวเลือกสั้น ๆ (client component) |
| `PageHeader` | หัวหน้าเพจ — ทุก route ใช้ตัวนี้ ชื่อหน้าจึงอยู่ตำแหน่ง y เดียวกันเสมอ |

**คลาสที่ใช้ได้:** `.card`, `.card-header`, `.chip`, `.surface`, `.label-xs`,
`.table-data` (+ เซลล์ `.num`), `.ring-hairline`, `.brand-text`,
`.skeleton` (shimmer), `.stagger` (ลูกไล่กัน fade-in)

### 2.3 เปลือกแอป (AppShell)

ไฟล์: [`src/components/layout/AppShell.tsx`](src/components/layout/AppShell.tsx)

- Sidebar จัดกลุ่มเป็น 3 หมวด (ผลการเล่น / วิเคราะห์ / ระบบ) แทนลิสต์แบนยาว 6 อัน
- สถานะ active มีทั้งพื้น teal-dim และ **แถบเส้นด้านซ้าย** (ไม่พึ่งแค่สีอย่างเดียว)
- โลโก้เป็น gradient แบรนด์
- ท้าย sidebar เป็นบล็อกโปรไฟล์ + `RankBadge` (ดึง `season_rank` เข้ามาใหม่)

---

## 3. ระบบใหม่ที่เพิ่ม

| ที่ไหน | ระบบ | แหล่งข้อมูล |
|---|---|---|
| ทั้งแอป | **Command Palette (⌘K / Ctrl-K)** — กระโดดไปหน้าต่าง ๆ, ค้นฮีโร่แบบ fuzzy, วาง match ID เพื่อเปิดแมตช์ | — |
| `/match/[id]` | **กราฟโอกาสชนะระหว่างเกม** — พลิกให้เป็นมุมมองฝั่งของผู้เล่นเสมอ เส้น 50% จึงแปลว่า "สูสี" จริง ๆ | `match.winRates` |
| `/match/[id]` | **ไทม์ไลน์ออบเจกทีฟ** — ป้อมที่ล้ม + first blood บนแกนเดียว จัดฝั่งตาม *ทีมที่ทำได้* | `towerDeaths`, `firstBloodTime` |
| `/match/[id]` | **ลำดับการซื้อของ** — เรียงตามเวลาจริง มีตัวกรองซ่อนของกิน | `stats.itemPurchases` |
| `/match/[id]` | **แผนที่การวางวอร์ด** — วาดภูมิประเทศเป็น SVG กรองตามทีมและช่วงเวลาได้ | `stats.wards` |
| `/` | **Session tracker + กราฟความล้า** — อัตราชนะแยกตามลำดับเกมในหนึ่งวัน | RPC `get_session_winrate` (มีอยู่แล้วแต่ไม่เคยถูกเรียกใช้) |

### หมายเหตุการออกแบบที่ตั้งใจ

- **กราฟโอกาสชนะ:** API ส่งมาเป็นโอกาสชนะของฝั่ง Radiant ถ้าแสดงดิบ ๆ คนที่เล่นฝั่ง Dire
  จะเห็นเกมที่ตัวเองบี้ขาดกลายเป็นเส้นติดพื้น จึงพลิกเป็น `1 - v` ตามฝั่งผู้เล่น
- **ไทม์ไลน์ออบเจกทีฟ:** วาง marker ไว้ฝั่งทีมที่ *ทำแต้มได้* ไม่ใช่ทีมที่เสียป้อม
  กำแพง marker กองอยู่ข้างเดียวจึงอ่านได้ทันทีว่า "ทีมนั้นยึดแมพ"
- **แผนที่วอร์ด:** ใช้ SVG วาดเอง (เลน แม่น้ำ ฐาน) ไม่ใช้ภาพ minimap จริง
  เพราะขนาดเท่านี้ภาพจริงจะเละอ่านไม่ออก และ landmark ที่จำเป็นมีแค่ไม่กี่อย่าง
  Observer = จุดทึบ, Sentry = วงกลมกลวง → รูปทรงบอกชนิดได้โดยไม่ต้องพึ่งสี
- **กราฟความล้า:** ช่องที่ตัวอย่างน้อยกว่า 5 เกม วาดเป็นแท่งกลวงแทนที่จะซ่อน
  เพื่อให้อ่านออกว่า "ข้อมูลยังไม่พอ" ไม่ใช่หายไปเงียบ ๆ

---

## 4. งาน consistency ที่ทำเพิ่ม

- เปลี่ยน `accent-blue` → `accent-teal` ใน 25 ไฟล์ (UI chrome) โดย **เว้น 4 ไฟล์ที่เป็นสีของเส้นกราฟ**
- `RangeSelector` และ `TeamNetWorthChart` เปลี่ยนมาใช้ `SegmentedControl` ตัวเดียวกัน
- ตาราง `/matches`, `MatchTable`, `TeamScoreboard` ใช้ `.table-data` + `HeroCell` + `ResultBadge` ร่วมกัน
- ทุก route ใช้ `PageHeader` เหมือนกัน
- `TeamScoreboard` ตัดคอลัมน์ Lvl ทิ้ง เพราะเลเวลไปอยู่บนป้ายมุมรูปฮีโร่แล้ว (ตามสกอร์บอร์ดในเกม)
- เพิ่ม `prefers-reduced-motion` ครอบ animation ตกแต่งทั้งหมด
- เพิ่ม `tabular-nums` กับตารางทุกอัน ตัวเลขจะได้ไม่กระตุกระหว่างแถว

---

## 5. การตรวจสอบ

ตรวจผ่าน route ชั่วคราว `/dev` (ลบทิ้งแล้ว, middleware คืนสภาพเดิมแบบ byte-identical)

**ผ่าน:**

- แผงใหม่ทั้ง 4 ของหน้าแมตช์ เรนเดอร์ด้วยข้อมูล STRATZ จริงของแมตช์ `8961414182`
  - วอร์ด 115 อัน พิกัดอยู่ในขอบเขตแผนที่ทั้งหมด ไม่มีตัวไหนถูก clamp
  - ตัวกรองทีมทำงาน: 115 = Radiant 64 + Dire 51
  - ตัวกรองช่วงเวลาทำงาน: 0-10m = 24, 25m+ = 59, ทั้งเกม = 115
  - กราฟโอกาสชนะ 56 จุด จบที่ 57%
  - ตัวกรองไอเทม: ไอเทมหลัก 205 → ทั้งหมด 438
- Command palette: ⌘K เปิดได้ โฟกัสเข้า input, ล็อก scroll, `sf` → Shadow Fiend,
  `8961414182` → ลิงก์เปิดแมตช์
- รูปภาพ 332 รูป เสียศูนย์รูป
- ไม่มี horizontal overflow ทั้งจอ desktop และ mobile
- Console สะอาดใน tab ใหม่
- ทุก route ตอบ 200
- `tsc --noEmit`, `next lint`, `next build` ผ่านหมด

**บั๊กที่เจอระหว่างทางและแก้แล้ว:**
พิมพ์ `"am"` ใน command palette แล้วได้ Shadow Sh**am**an มาก่อน Anti-Mage
เพราะการ match แบบ substring ได้คะแนนสูงกว่าตัวย่อ → ปรับลำดับคะแนนให้ตัวย่อชนะ substring กลางคำ

**ยังไม่ได้ตรวจ:**
ตารางหน้า `/matches` ที่ refactor ใหม่ ผ่านแค่ type-check/lint และเห็นแต่ empty state
เพราะบัญชีที่ใช้ทดสอบไม่มีแถวที่มองเห็นได้ภายใต้ RLS — ควรดูด้วยตาอีกทีตอน login จริง

---

## 6. ไฟล์ที่เปลี่ยน

**เพิ่มใหม่ (15 ไฟล์):**

```
src/components/ui/Card.tsx
src/components/ui/Badge.tsx
src/components/ui/Meter.tsx
src/components/ui/Sparkline.tsx
src/components/ui/HeroAvatar.tsx
src/components/ui/StatTile.tsx
src/components/ui/PageHeader.tsx
src/components/ui/SegmentedControl.tsx
src/components/ui/Tooltip.tsx
src/components/layout/CommandPalette.tsx
src/components/match/WinProbabilityChart.tsx
src/components/match/ObjectiveTimeline.tsx
src/components/match/ItemBuildTimeline.tsx
src/components/match/WardMap.tsx
src/components/overview/SessionTracker.tsx
```

**แก้ไข:** 46 ไฟล์ (+1,443 / −878 บรรทัด) — รวม `tailwind.config.ts`, `globals.css`,
`AppShell.tsx`, ทุกหน้า route, `stratz-match.ts`, `utils.ts`, `database.ts`, `DEVELOPMENT.md`

**ยังไม่ commit** — `package.json` / `.gitignore` ที่ขึ้นว่าแก้ไข เป็นของเดิมที่ dirty
อยู่ก่อนเริ่มงาน (Vercel Speed Insights) ไม่ได้แตะ

---

## 7. สิ่งที่ทำต่อได้

ข้อมูลที่ดึงมาแล้วแต่ยังไม่ได้ใช้ อยู่ใน `player.stats` — เอาไปทำแผงเพิ่มได้ไม่ยาก:

- `heroDamageReport` — ดาเมจที่ทำ/รับ แยกตามเป้าหมายและตามสกิล (STRATZ มีแผงนี้จริง)
- `deathEvents` — ตำแหน่งที่ตาย, ทองที่เสีย, `isBurst`
- `runes` — การเก็บรูน
- `impPerMinute` — IMP รายนาที

**กับดักที่ควรรู้ (บันทึกไว้ใน `DEVELOPMENT.md` แล้ว):**

- **ห้ามรัน `npm run build` ตอนที่ `next dev` ยังทำงานอยู่** — ทั้งคู่เขียน `.next/`
  แล้ว build จะทับ chunk map ของ dev server ทำให้ route ที่คอมไพล์ทีหลัง 500 ด้วย
  `Cannot find module './vendor-chunks/@supabase.js'` → ต้องหยุด dev server, `rm -rf .next`, แล้วเริ่มใหม่
- เวลาเช็ค console error ให้เปิด **tab ใหม่เสมอ** — tab เดิมจะค้าง error เก่าจาก HMR ไว้
  (รอบนี้เจอ `ReferenceError: accent is not defined` ค้างอยู่ ทั้งที่โค้ดปัจจุบันเรนเดอร์ผ่านแล้ว)
